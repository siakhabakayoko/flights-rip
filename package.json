{
  "name": "flights-rip",
  "version": "1.0.0",
  "description": "Skyscanner clone build with MongoDB, Express, Next.js, React",
  "main": "index.js",
  "repository": "https://github.com/signofactory/flights-rip",
  "author": "Signofactory",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.2.0"
  },
  "scripts": {
    "dev": "concurrently \"cd backend && npm install && npm run dev\" \"cd frontend && npm install && npm run dev\"",
    "build": "concurrently \"cd backend && npm install\" \"cd frontend && npm install && npm run build\""
  }
}
